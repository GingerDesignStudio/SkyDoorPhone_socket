{
  "name": "socket-server",
  "version": "1.0.0",
  "description": "A simple socket server",
  "main": "script.js",
  "scripts": {
    "start": "node app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "npm": "^10.2.5",
    "socket.io": "^4.7.2",
    "express": "^4.18.2"
  }
}
